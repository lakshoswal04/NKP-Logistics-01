# NKP Logistics — Next.js frontend environment
# Copy to frontend/.env and fill in real values.

NEXT_PUBLIC_API_URL=http://localhost:8000

# Google Maps JS SDK (tracking map upgrades from the stylized mock map when set)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
